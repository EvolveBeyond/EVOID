[service]
name = "evoid-api"
version = "1.0.0"

[runtime]
adapter = "asgi"
host = "0.0.0.0"
port = 8000

[engines]
schema = "native"
storage = "memory"
cache = "memory"
serializer = "json"
logger = "loguru"

[pipeline]
processors = ["validate", "authorize"]
